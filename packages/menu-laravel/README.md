# @affino/menu-laravel

> Laravel + Livewire bindings for `@affino/menu-core`. This package exposes a DOM bootstrapper that turns declarative Blade markup into fully interactive menus while participating in the shared overlay kernel stack.

## Status

🚧 Work in progress. The initial release focuses on the JavaScript bridge; Livewire view components and full demos will follow.
