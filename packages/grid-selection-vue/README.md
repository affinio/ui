# @affino/grid-selection-vue

Vue 3 helpers for wiring `@affino/grid-selection-core` controllers into reactive components.

- Headless store abstraction with immutable snapshots
- Vue composable for subscribing to store updates inside setup functions
- No DOM or rendering assumptions – works with any grid/table implementation
